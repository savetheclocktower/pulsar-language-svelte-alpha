'use babel';

import LanguageSvelteCustomView from '../lib/language-svelte-custom-view';

describe('LanguageSvelteCustomView', () => {
  it('has one valid test', () => {
    expect('life').toBe('easy');
  });
});
